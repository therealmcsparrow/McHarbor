{
  "label": "Container Exec",
  "description": "Execute a command inside a running container",
  "config": {
    "environment": "Omgeving",
    "container": "Container",
    "command": "Command",
    "workdir": "Werkmap"
  }
}
